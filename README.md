# Learning Angular 2 components

Let's learn about iterators, `*ngFor`, conditional statements like `*ngIf` and much more.

## Install & Run

Install the latest dependencies using 

```
$ npm install
$ npm start
```
